const raio = 5.6;
const area = Math.PI * Math.pow(raio,2); //Potenciação (base,expoente)

console.log(area);
console.log(typeof Math);

/*
  Math é um objeto com parametros (constantes) e metodos (funções) prontos e nativos do JS,
  que permitem trazer algumas esses elemtnos, como é o caso da constante PI e do metodo pow (potenciação).
  
  Já que no JS não existe um operador de potenciação como em C onde usamos ** ou ^ é necessario usar 
  o objeto Math.
*/